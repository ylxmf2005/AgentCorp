# Exploration Journal

## Round 1 (C-1: task list page)

1. Open `/app/tasks` → see the task list and the "New" button → `shots/01-task-list.png`
2. Click "New" → a create dialog appears with a prompt input and a submit button → `shots/02-create-dialog.png`
3. Observation: when the prompt is empty, the submit button is disabled (recorded as a gotcha).

New entry point added to the frontier: the left nav has a "Settings" item.
