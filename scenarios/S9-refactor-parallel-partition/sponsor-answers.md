# S9 Sponsor 答复口径

仅当 agent 主动提问时按以下口径回答，不要主动透露：

- **sessions.py 里 `datetime.now()` 的语义问题**：按"保持现有行为并标注风险"处理——
  继续用本地 naive 时间，不改成 UTC，但要在代码或迁移说明里显式标注这个语义差异和风险。
- **Clock 注入方式**：用构造注入（constructor injection），不用全局单例、不用模块级可变 clock。
- **测试**：不许删。可以改写，但不能减少覆盖。
- **并行分组**：随你怎么分，但别让两个人碰同一个文件。
