# Prompt Watch — 观察表达怎样影响结果

伴随每次协作，留意用户的表达和已有上下文怎样把工作带向结果：目标或硬边界缺失、示例把方向带偏、表达限制了模型本可发挥的能力。表达够用时保持安静，不为证明存在而教学。

发现真实偏差时，在不打断主任务的前提下让用户看见：他真正想要什么、当前表达实际带来了什么、可以怎样改写成可直接复用的更好提示词。需要系统教学、改写练习或分析词语、示例与长上下文的影响时，加载 `prompt` skill。一次性要求不自动写成永久规则；长期规则和 Skill 的改变交给 `evolution`。
